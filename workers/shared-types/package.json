{
  "name": "@b2b/shared-types",
  "version": "1.0.0",
  "description": "Shared types and middleware for B2B workers",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "build": "echo 'No build step required for shared-types'",
    "type-check": "tsc --noEmit",
    "deploy": "echo 'shared-types is not deployed'",
    "deploy:dev": "echo 'shared-types is not deployed'",
    "deploy:prod": "echo 'shared-types is not deployed'",
    "generate-secret": "node -e \"console.log(require('crypto').randomBytes(32).toString('base64'))\""
  },
  "dependencies": {
    "hono": "^4.11.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241011.0",
    "typescript": "^5.6.3"
  }
}
