{
  "name": "inventory-service",
  "version": "1.0.0",
  "description": "B2B Inventory Management Service - Product CRUD and stock management",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --env dev --port 8791",
    "build": "echo 'No build step required for inventory-service'",
    "type-check": "tsc --noEmit",
    "deploy": "wrangler deploy --config wrangler.toml",
    "deploy:dev": "wrangler deploy --config wrangler.toml --env dev",
    "deploy:prod": "wrangler deploy --config wrangler.toml",
    "test": "vitest"
  },
  "keywords": [
    "cloudflare-workers",
    "inventory",
    "products",
    "d1"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@b2b/db": "workspace:*",
    "drizzle-orm": "^0.44.3",
    "hono": "^4.11.4",
    "nanoid": "^5.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "wrangler": "^4.52.1"
  }
}
