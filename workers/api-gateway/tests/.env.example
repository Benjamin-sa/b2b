# Environment variables for integration tests
# Copy this file to .env and fill in the actual values
# ⚠️ NEVER commit .env to git (already in .gitignore)

# API Gateway Dev Worker URL (optional - default shown below)
API_GATEWAY_DEV_URL=https://b2b-api-gateway-dev.benkee-sauter.workers.dev

# Test Admin User Credentials
# Create this user in dev database with role='admin' and is_verified=1
# Tests will automatically login with these credentials and get fresh JWT tokens
TEST_ADMIN_EMAIL=admin@test.local
TEST_ADMIN_PASSWORD=YourTestPassword123!
